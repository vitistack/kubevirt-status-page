apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubevirt-status-page.fullname" . }}
  labels:
    {{- include "kubevirt-status-page.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubevirt-status-page.selectorLabels" . | nindent 4 }}
